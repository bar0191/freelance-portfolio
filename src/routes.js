import React from 'react';
import {Route, IndexRoute, Redirect} from 'react-router';
import App from './App';


export default (
    <Route>
        <Route component={App} path='/'>
        </Route>
    </Route>
);
